use vortex::array::ArrayRef;

fn main() {
    // This is just to see what traits are in scope by default or where Array is.
    // I'll try to find it via compiler errors.
    let _a: vortex::array::Array = todo!();
}
